const {test}=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');const path=require('node:path');
test('All 12 chapters have four bilingual subsections and proposed problems with hints and solutions',()=>{const c={};c.window=c;vm.createContext(c);for(const f of ['lessons.js','recovered-examples.js'])vm.runInContext(fs.readFileSync(path.join(__dirname,'..',f),'utf8'),c);assert.equal(Object.keys(c.LESSONS).length,12);for(const [id,d] of Object.entries(c.LESSONS)){assert.equal(d.sections.length,4,id);assert.ok(d.exercises.length>=1,id);for(const s of d.sections)for(const lang of ['es','en']){assert.ok(s.title[lang]);assert.ok(s.body[lang].length>=2);}for(const e of d.exercises)for(const key of ['prompt','hint','solution'])for(const lang of ['es','en'])assert.ok(e[key][lang]);}assert.equal(c.LESSONS.cap2.recovered.length,5);});
test('Recovered original hourly example and two-step loss example',()=>{const p=[400,380,350,420,710,820,870,850,830,910,780,540];assert.equal(p.reduce((a,b)=>a+b,0)*2,15720);let load=(8+16*Math.sqrt(2.5/12))/24;assert.ok(Math.abs(load-.63762)<.00001);let actual=136/288;assert.ok(load*load<=actual&&actual<=load);assert.ok(870/.85>1000);});
